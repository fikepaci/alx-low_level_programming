#include "holberton.h"

/**
* _puts - prints a string, followed by a new line, to stdout.
* @str: string to be printed.
*/

void _puts(char *str)
{
while (*ch)
{
_putchar(*str);
str++;
}

_putchar('\n');

return (0);
}
